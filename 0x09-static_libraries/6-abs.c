#include "main.h"

/**
 * _abs - It computes the absolute value of an integer
 * @x: the int to check
 * Return: the value of int
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	return (-x);
}
