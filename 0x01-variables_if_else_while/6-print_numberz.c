#include <stdio.h>

/**
 * main - Print numbers from 0 to 9 without using the 'char' variable.
 *
 * Return: 0
 */

int main(void)
{
	int x;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
